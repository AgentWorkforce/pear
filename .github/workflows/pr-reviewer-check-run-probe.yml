name: pr-reviewer check-run probe

on:
  pull_request:
    paths:
      - ".github/workflows/pr-reviewer-check-run-probe.yml"

jobs:
  forced-failure:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1
